{
  "devDependencies": {
    "web-ext": "^4.1.0",
    "webextension-polyfill": "^0.6.0"
  },
  "scripts": {
    "start:firefox": "web-ext run -t --firefox=firefoxdeveloperedition",
    "start:chrome": "web-ext run -t chromium"
  }
}
